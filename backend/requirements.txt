# ── Core conversion engine ──────────────────────────────────────────────────
# Named extras (not [all]) to avoid heavy deps (ONNX, Whisper, Azure) that OOM
# on Render's 512 MB free tier. The [xlsx] extra is what registers the Excel
# converter inside MarkItDown even when openpyxl is present.
#   [pdf]  → pdfminer.six, pdfplumber      [docx] → mammoth
#   [pptx] → python-pptx                   [xlsx] → openpyxl
#   [xls]  → xlrd (legacy .xls)
markitdown[pdf,docx,pptx,xlsx,xls]>=0.1.0

# HTML → Markdown support used by MarkItDown's HTML path
beautifulsoup4
markdownify

# ── API framework ───────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ── Rate limiting ───────────────────────────────────────────────────────────
slowapi>=0.1.9

# ── Image metadata fallback (no OCR on free tier) ───────────────────────────
Pillow>=10.0.0

# ── Dev / test (installed in CI; harmless in prod) ──────────────────────────
pytest>=8.0.0
httpx>=0.27.0
# Writer libs used only to generate regression fixtures (not runtime deps).
python-docx>=1.1.0
python-pptx>=0.6.23
