# Core conversion library
# Using specific extras instead of [all] to reduce memory footprint (~500MB → ~150MB).
# Dropped: onnxruntime (image description — we use RapidOCR instead),
#          speechrecognition/pydub (audio — rarely used, very heavy),
#          magika (content detection — heavy).
markitdown>=0.1.0
pdfminer.six
pypdfium2
pdfplumber
python-pptx
openpyxl
xlrd
mammoth
beautifulsoup4
markdownify

# API framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Image OCR — pure Python, ONNX Runtime backend, no system binaries required
rapidocr-onnxruntime>=1.4.0

# Image processing (resize before OCR + metadata fallback)
Pillow>=10.0.0
