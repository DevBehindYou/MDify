# Core conversion library (individual extras, NOT [all] — saves ~350MB of RAM)
markitdown>=0.1.0
pdfminer.six
pypdfium2
pdfplumber
python-pptx
openpyxl
xlrd
mammoth
beautifulsoup4
markdownify

# API framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Image processing (metadata + basic text-in-image detection)
Pillow>=10.0.0
